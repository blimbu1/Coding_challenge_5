Challenge 5. 

Done in python using pandas DataFrame. A little preprocessing of CSV needed prior to loading the file in python pandas DF.
Also attached is the SQL query that should get the same results.